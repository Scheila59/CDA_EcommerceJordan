{# Navigation #}
<nav class="navbar navbar-dark bg-dark navbar-expand-lg">
    <div class="container">
        <a class="navbar-brand d-flex align-items-center" href="{{ path('app_home') }}">
            <img src="https://e7.pngegg.com/pngimages/153/439/png-clipart-air-jordan-logo-jumpman-air-jordan-nike-air-max-shoe-nike-sneakers-adidas-thumbnail.png" 
                alt="Jordan Logo" 
                width="40" 
                height="40" 
                class="d-inline-block me-2">
            Jordan Store
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0 ms-lg-4">
                <li class="nav-item">
                    <a class="nav-link hover-lift" href="{{ path('app_home') }}">
                        <i class="bi bi-house me-1"></i>Accueil
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link hover-lift" href="{{ path('app_product_catalog') }}">
                        <i class="bi bi-shop me-1"></i>Produits
                    </a>
                </li>
            </ul>
            <ul class="navbar-nav ms-auto">
                <form class="d-flex me-3">
                    <button class="btn btn-outline-light" type="submit">
                        <i class="bi bi-cart-fill me-1"></i>
                        Panier
                        <span class="badge bg-light text-dark ms-1 rounded-pill">0</span>
                    </button>
                </form>
                
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle hover-lift" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        <i class="bi bi-person-circle me-1"></i>Compte
                    </a>
                    
                    <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
                        {% if app.user %}
                            <li><span class="dropdown-item-text">Connecté en tant que {{ app.user.email }}</span></li>
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <a class="dropdown-item hover-lift" href="{{ path('app_logout') }}">
                                    <i class="bi bi-box-arrow-right me-2"></i>Déconnexion
                                </a>
                            </li>
                        {% else %}
                            <li>
                                <a class="dropdown-item hover-lift" href="{{ path('app_login') }}">
                                    <i class="bi bi-box-arrow-in-right me-2"></i>Connexion
                                </a>
                            </li>
                            <li>
                                <a class="dropdown-item hover-lift" href="{{ path('app_register') }}">
                                    <i class="bi bi-person-plus me-2"></i>Inscription
                                </a>
                            </li>
                        {% endif %}
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>